id: dbt_run
namespace: project

tasks:
  - id: dbt_run
    type: io.kestra.core.tasks.scripts.Bash
    commands:
      - docker exec -e GOOGLE_APPLICATION_CREDENTIALS="/opt/src/pipeline/gcs.json" /project_dbt_runner bash -c "dbt run"
      
